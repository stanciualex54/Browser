//---------------------------------------------------------------------------


#pragma hdrstop

#include "TagTitlu.h"

//---------------------------------------------------------------------------

#pragma package(smart_init)
Titlu :: Titlu(){

        Form1->Edit->Clear();
        string start = "<body>";
        string end = "</body>";
        string text;
        Form1->Caption = gasire(text, start, end).c_str();
}
